import { CellResizeDirective } from "./cell-resize.directive";

describe('CellResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new CellResizeDirective();
    expect(directive).toBeTruthy();
  });
});
